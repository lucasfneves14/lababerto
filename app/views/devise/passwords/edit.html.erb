        <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class:"login100-form validate-form" }) do |f| %>
          <span class="login100-form-title p-b-43">
            Mudar a Senha
          </span>
          
          <%= f.error_notification %>
          <% flash.each do |key, value| %>
              <%= content_tag :div, value, class: "flash #{key}", style:'color:red' %>
            <% end %>

          <%= f.input :reset_password_token, as: :hidden %>
          <%= f.full_error :reset_password_token %>
          
          
          <div class="wrap-input100 validate-input" data-validate="A senha é necessária">
            <input class="input100 required" type="password" name="user[password]" id="user_password" required='required' aria-autocomplete="list">
            <span class="focus-input100"></span>
            <span class="label-input100">Senha</span>
          </div>

          <%=f.error :password%>


          <div class="wrap-input100 validate-input form-group password required user_password_confirmation" data-validate="A senha é necessária">
            <input class="input100 required" type="password" name="user[password_confirmation]" id="user_password_confirmation" required='required' aria-autocomplete="list">
            <span class="focus-input100"></span>
            <span class="label-input100">Confirme a Senha</span>
          </div>

          <%=f.error :password_confirmation%>

          <div class="flex-sb-m w-full p-t-3 p-b-32">
            <div class="contact100-form-checkbox">
              <input type="hidden" name="user[remember_me]" value='0'>
              <input class="input-checkbox100" id="user_remember_me" type="checkbox" name="user[remember_me]" value="1">
              <label class="label-checkbox100" for="user_remember_me">
                Me mantenha logado
              </label>
            </div>

            <div>
              <%= render "devise/shared/links" %>
            </div>
          </div>
      

          <div class="container-login100-form-btn">
            <%= f.button :submit, "Mudar Senha",class:"login100-form-btn"%>
          </div>
          
        <%end%>

 